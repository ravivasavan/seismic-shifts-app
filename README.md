# gh-pages — placeholder branch

This branch only exists because GitHub does not allow programmatic
deactivation of GitHub Pages once enabled. The actual site lives in
the sibling repo
[ravivasavan/seismic-shifts-site](https://github.com/ravivasavan/seismic-shifts-site).

The HTML files here are tiny meta-refresh redirects pointing at
that canonical site, so anyone who hits the old URL ends up in the
right place.
